# Rendering report.html from the bundled results/ needs only:
numpy>=1.24
matplotlib>=3.6

# Live re-runs (bench_eps_sweep.py / bench_n_sweep.py / --live) additionally need
# a CUDA build of PyTorch and the CPU baseline stack:
torch>=2.1            # CUDA build; see https://pytorch.org for the right wheel
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
networkx>=3.0
scipy>=1.10
pyballmapper>=0.3.7
pytest>=7.0
# cupy>=13.0          # optional: on-GPU coverage/edge post-processing (faster)
